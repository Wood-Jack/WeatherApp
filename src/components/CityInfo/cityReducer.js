
/* eslint-disable linebreak-style */
const defaultState = {

};



export default function cityReducer(state = defaultState, action) {
  const { type } = action;

  switch (type) {

    default:

      return state;

  }
}
